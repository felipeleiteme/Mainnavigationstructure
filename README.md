
  # Main Navigation Structure

  This is a code bundle for Main Navigation Structure. The original project is available at https://www.figma.com/design/V2HbMYt8iPmL1r3fsZBZcg/Main-Navigation-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  